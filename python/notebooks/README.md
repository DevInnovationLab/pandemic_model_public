# Python Notebooks

Exploratory development notebooks (`.ipynb` files). These are not part of the
model workflow and are not guaranteed to run without manual setup.

Notebooks cover topics including economic loss modeling, wastewater surveillance,
sensitivity analysis exploration, and distribution fitting experiments.

To run: activate the Poetry environment (`cd python && poetry shell`), then
launch Jupyter (`jupyter notebook`) from the repo root.
